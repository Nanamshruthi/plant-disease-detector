# Core dependencies with exact versions
flask==2.3.2
gunicorn==20.1.0
tensorflow-cpu==2.10.0
pillow==10.0.0
numpy==1.22.4  # Last version fully compatible with TF 2.10
gdown==4.6.6
werkzeug==2.3.7

# Additional constraints to prevent conflicts
--use-deprecated=legacy-resolver
